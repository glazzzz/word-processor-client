#Kafka consumer-producer configs
producer.kafka-uri=direct:producer
consumer.kafka-uri=mock:consumer

#Cassandra consumer configs
producer.cassandra-uri=mock:consumer

#Elasticsearch consumer configs
update.elastic-uri=mock:consumer
search.elastic-uri=mock:consumer